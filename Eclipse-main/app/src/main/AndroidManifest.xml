<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".EclipseApplication"
        android:label="Eclipse"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.Eclipse"
        android:allowBackup="false"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config"
        android:hardwareAccelerated="true"
        android:largeHeap="true"
        tools:targetApi="34">

        >

        <!--
            Section 33.6: Disable debugging in release builds.
            Debug builds automatically override this with debuggable=true.
            In release APK this is always false — no analysis tools can attach.
        -->

        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Eclipse.Splash"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|keyboard|smallestScreenSize|uiMode"
            android:screenOrientation="portrait">

            >

            <!--
                Section 35.3: configChanges prevents activity recreation on
                orientation change, keyboard show/hide, and screen size changes.
                This is CRITICAL for Section 35 — prevents unnecessary app restarts.
                Section 38.5: orientation must be listed so WebView fullscreen can
                change orientation without destroying/recreating activity.
            -->

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.Eclipse"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|keyboard|smallestScreenSize|uiMode"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize">

            >

            <!--
                Section 35.3: SAME configChanges as SplashActivity.
                Prevents activity recreation when:
                - User rotates phone (Section 38.5)
                - Keyboard shows/hides (Section 28)
                - User switches apps and returns (Section 35)
                This is the core fix for app restart bug.
            -->

            <!--
                Section 38.5: Start in portrait, WebView chrome client will
                change orientation programmatically during video fullscreen.
            -->

        </activity>

        <!--
            Section 18: FileProvider for camera image URIs in AI chat
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
